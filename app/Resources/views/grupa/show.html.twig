{% extends 'base.html.twig' %}

{% block body %}
    <h1>Grupa</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grupa.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ grupa.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('grupa_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('grupa_edit', { 'id': grupa.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
